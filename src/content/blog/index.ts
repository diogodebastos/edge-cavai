/* Generated by src/lib/generate-blog-index.mjs — do not edit. */
export type BlogEntry = { title: string; excerpt: string; minutes: number; html: string };
const posts: Record<string, BlogEntry> = {
  "blogpost-6": {
    "title": "Velvet Blum: a social network with no algorithm and no ads",
    "excerpt": "A weekend experiment in giving the feed back to the user.",
    "minutes": 5,
    "html": "<h1>Velvet Blum: a social network with no algorithm and no ads</h1>\n<p><em>A weekend experiment in giving the feed back to the user.</em></p>\n<hr>\n<p>Every social network I&#39;ve used in the last decade has the same loop: you follow people, an algorithm decides what you actually see, and ads are slotted in between. The follow graph stops being the feed. It becomes a <em>suggestion</em> the algorithm overrides.</p>\n<p>Velvet Blum is the opposite of that. <strong>You curate your network. Your network is your feed.</strong> No ranking, no recommendations, no ads. Reverse-chronological, always.</p>\n<p>Live: <a href=\"https://velvet-blum.pages.dev/\">velvet-blum.pages.dev</a>.</p>\n<p><img src=\"/images/blog/bp6/feed.jpg\" alt=\"feed\"></p>\n<h2>The lens</h2>\n<p>The interesting primitive isn&#39;t the feed — it&#39;s the <strong>lens</strong>.</p>\n<p>Visit <code>/u/&lt;handle&gt;/lens</code> and you see <em>that user&#39;s feed</em> — the posts they would see, in their reverse-chronological order, through their follow graph. Read-only. You can&#39;t react or comment as them. But every post has a tiny <code>+ follow @author</code> button so you can copy accounts you didn&#39;t know about into your own network.</p>\n<p>It turns curation into a public good. Instead of an algorithm doing taste-matching for you, you borrow taste from a person you trust. One click and a stranger&#39;s curator becomes a contributor to yours.</p>\n<p><img src=\"/images/blog/bp6/lens.jpg\" alt=\"lens\"></p>\n<h2>Encrypted DMs</h2>\n<p>The other thing I wanted was DMs the server can&#39;t read.</p>\n<ul>\n<li>Per-device <strong>X25519</strong> keypair. Private key wrapped with a passphrase via <strong>Argon2id</strong> + libsodium <code>secretbox</code>. Server never sees the passphrase or the unwrapped key.</li>\n<li>Messages sealed client-side using the recipient&#39;s public key (<code>crypto_box_seal</code>). Server stores ciphertext only.</li>\n<li>A <strong>second sealed copy</strong> is written under the sender&#39;s own public key, so the sender can also read their own history from any device they unlock.</li>\n<li>Realtime fanout via a <strong>Durable Object per conversation</strong> with WebSocket subscribers.</li>\n<li>A user may DM another iff the recipient follows the sender, and neither party blocks the other. No cold inbox, no spam vector.</li>\n</ul>\n<p><img src=\"/images/blog/bp6/dm.jpg\" alt=\"encrypted DMs\"></p>\n<p>If the D1 database leaks tomorrow, the messages stay opaque. The keys to read them never left the clients.</p>\n<h2>Security</h2>\n<p>&quot;Isn&#39;t a vibecoded social app a security disaster?&quot; Fair question. Two pieces are worth being concrete about: account access and message contents.</p>\n<p><strong>Account access.</strong> There are no passwords to leak or reuse. Auth is magic-link only: you put in your email, the API mints a single-use token, you click it, and the server creates a session. The session token is 32 bytes from <code>crypto.getRandomValues</code> (256 bits of entropy), stored server-side in Workers KV, and handed back as an <code>httpOnly</code>, <code>SameSite=Lax</code>, <code>Secure</code> cookie. JavaScript can&#39;t read it, so an XSS bug can&#39;t exfiltrate it; SameSite blocks the obvious CSRF shapes. Magic-link issuance is rate-limited (5 per email per 10 minutes), and links are single-use and time-bound. The practical floor of your account security is your email inbox.</p>\n<p><strong>How login works</strong>, end to end:</p>\n<ol>\n<li>Client <code>POST /auth/magic-link</code> with an email. The API generates a token, stores <code>(token, email, expiresAt)</code> in D1, and emails a link via Resend.</li>\n<li>Client <code>POST /auth/consume</code> with the token. The API marks the magic-link row consumed (single-use), looks up or creates the user, mints a session token, writes <code>s:&lt;token&gt; → userId</code> to Workers KV with a 30-day TTL, and sets the <code>vb_session</code> cookie.</li>\n<li>Every subsequent request goes through <code>sessionMiddleware</code>, which reads the cookie and resolves it back to a <code>userId</code> via KV. Protected routes wrap that with <code>requireAuth</code>.</li>\n<li>Logout deletes the KV entry and clears the cookie, immediately invalidating the session everywhere.</li>\n</ol>\n<p>The whole auth surface is small enough to read in one sitting: <a href=\"https://github.com/diogodebastos/velvet-blum/blob/main/apps/api/src/routes/auth.ts\"><code>apps/api/src/routes/auth.ts</code></a> for the login flow, and <a href=\"https://github.com/diogodebastos/velvet-blum/blob/main/apps/web/src/lib/crypto.ts\"><code>apps/web/src/lib/crypto.ts</code></a> (~150 lines) for every line of crypto in the app.</p>\n<p><strong>Message contents.</strong> The DM keys live on your device, not on the server. The X25519 keypair is generated client-side; the private key is wrapped with an Argon2id-derived key (<code>OPSLIMIT_MODERATE</code> / <code>MEMLIMIT_MODERATE</code>, random per-user salt) using libsodium <code>secretbox</code>, and only the <em>wrapped</em> blob is uploaded. Once unwrapped with your passphrase, the raw private key is cached in the browser&#39;s IndexedDB on that device. The server stores public keys, wrapped private keys, and sealed ciphertexts — nothing it can decrypt. A session hijacker on a fresh device sees encrypted blobs until they also know your passphrase.</p>\n<p><strong>What this doesn&#39;t give you.</strong> Sealed-box DMs use static keys, so there&#39;s no forward secrecy: if a device is fully compromised, past ciphertexts on that device become readable. There&#39;s no 2FA or active-session list yet, no &quot;log out everywhere&quot; button, and no re-auth prompt for sensitive actions. The crypto is standard libsodium primitives in roughly 150 lines (<code>apps/web/src/lib/crypto.ts</code>) — easy to audit, but not formally audited. Forward secrecy via ephemeral session keys, device management, and a passphrase-strength meter are the obvious next steps.</p>\n<h2>Stack</h2>\n<ul>\n<li><strong>Web</strong>: SvelteKit PWA on Cloudflare Pages</li>\n<li><strong>API</strong>: Hono on Cloudflare Workers</li>\n<li><strong>DB</strong>: D1 (SQLite) via Drizzle</li>\n<li><strong>Media</strong>: R2 for photos, Stream for video</li>\n<li><strong>Sessions</strong>: Workers KV</li>\n<li><strong>Email</strong>: Resend (magic-link auth — no passwords)</li>\n<li><strong>Realtime</strong>: Durable Objects for DM WebSocket fanout</li>\n<li><strong>Crypto</strong>: libsodium-wrappers (X25519 sealed-box, Argon2id KDF, secretbox)</li>\n</ul>\n<p>The whole thing is one pnpm workspace: <code>apps/api</code> (Hono), <code>apps/web</code> (SvelteKit), <code>packages/db</code> (Drizzle schema + migrations), <code>packages/schema</code> (shared Zod types), <code>infra/wrangler.toml</code>.</p>\n<h2>Why no algorithm</h2>\n<p>I&#39;m not against ranking in general — I&#39;m against ranking I didn&#39;t ask for. The algorithmic feed conflates two things: <em>what is new in my network</em> and <em>what someone thinks I want</em>. The first is a fact. The second is a guess. Velvet Blum only shows you the first. If you want a different selection, you build a different network — or you borrow one through a lens.</p>\n<p>It&#39;s also a much smaller surface. No ranking pipeline, no engagement metrics, no recommendation model, no ad inventory. The whole product is a couple of Workers, a D1, an R2, and a handful of Durable Objects.</p>\n<h2>What&#39;s next</h2>\n<ul>\n<li>Threading and quote-posts.</li>\n<li>Group DMs (same X25519 sealed-box construction, one envelope per recipient).</li>\n<li>Image-first compose flow on mobile.</li>\n<li>Federation is the obvious next question — but I want to live with the curated-only model for a while first.</li>\n<li>Security:<ul>\n<li>Forward secrecy for DMs via ephemeral session keys (X3DH-style handshake), so device compromise doesn&#39;t retroactively unlock history.</li>\n<li>Active-session list and a &quot;log out everywhere&quot; button backed by a per-user session index in KV.</li>\n<li>Optional TOTP 2FA on top of magic-link, and re-auth prompts for destructive actions (delete account, rotate keys).</li>\n<li>Passphrase strength meter at signup, plus a key-rotation flow that re-wraps the private key without invalidating message history.</li>\n<li>A formal third-party audit of <code>crypto.ts</code> and the auth surface before the app leaves &quot;weekend experiment&quot; status.</li>\n</ul>\n</li>\n</ul>\n<p>Try it: <a href=\"https://velvet-blum.pages.dev/\">velvet-blum.pages.dev</a>. If you make a network, share your lens URL.</p>\n"
  },
  "blogpost-5": {
    "title": "Wrangler ships your code. Something needs to wire your stack.",
    "excerpt": "A weekend project, an open question for Cloudflare.",
    "minutes": 5,
    "html": "<h1>Wrangler ships your code. Something needs to wire your stack.</h1>\n<p><em>A weekend project, an open question for Cloudflare.</em></p>\n<hr>\n<p>Claude Code can build a Cloudflare Worker in thirty seconds.</p>\n<p>Wiring it up takes thirty minutes.</p>\n<p>I want to talk about that gap. Where it comes from, why Agents Week 2026 didn&#39;t quite close it, and a small open-source tool I built — <code>binding-doctor</code> — that takes a stab at the missing piece.</p>\n<h2>The setup</h2>\n<p>I&#39;m a physics PhD turned data scientist. I love building things and I love Cloudflare. The agent-driven workflow Cloudflare has been pushing — Workers + Agents SDK + the API MCP server + the Skills plugin — feels like the right shape of the future. So I sat down this weekend with Claude Code and tried to build a small chat app on Workers using nothing but prompts.</p>\n<p>The code came out clean. A <code>fetch</code> handler, a Durable Object, a few <code>env.X</code> references for D1, KV, R2, a queue. Three minutes.</p>\n<p>Then came the part the agent couldn&#39;t do.</p>\n<pre><code>&gt; wrangler deploy\n✘ [ERROR] Database &quot;CHAT_DB&quot; missing database_id in wrangler.toml.\n</code></pre>\n<p>So I asked Claude. Claude told me to run <code>npx wrangler d1 create chat-db</code>, copy the UUID, paste it in. Then KV. Then R2. Then a queue. Then <code>wrangler secret put</code>. Each step a context switch. Each ID hand-carried from terminal to file. Twenty-five more minutes of plumbing.</p>\n<p>The agent could <em>write the program</em>. It could not <em>finish the program</em>.</p>\n<h2>What Cloudflare already shipped</h2>\n<p>Cloudflare Agents Week 2026 was a serious push at this whole problem space. Quick recap, because it&#39;s important — I&#39;m not pitching against it, I&#39;m pitching into one of the gaps it left:</p>\n<ul>\n<li><strong>Cloudflare API MCP</strong> — agents can hit any of 2500+ endpoints via two tools, <code>search</code> and <code>execute</code>.</li>\n<li><strong>Skills plugin</strong> — <code>wrangler</code> skill for command knowledge, <code>/cloudflare:build-agent</code> and <code>/cloudflare:build-mcp</code> slash-commands for scaffolding.</li>\n<li><strong>Stripe + Projects protocol</strong> — agents can autonomously create a Cloudflare account, register a domain, get an API token, and deploy. No human needed at the <em>account</em> layer.</li>\n<li><strong>Project Think</strong> — durable, actor-based primitives so agents <em>persist</em> across runs instead of being one-shot.</li>\n<li><strong>Durable Object Facets, Outbound Workers</strong> — better isolation primitives.</li>\n<li><strong>Enterprise MCP + Code Mode</strong> — observability, gateway, WAF wrapped around agent traffic at scale.</li>\n</ul>\n<p>It&#39;s a remarkable amount of stuff in one week. Most of the friction I named — <em>account creation</em>, <em>payment</em>, <em>token issuance</em> — is solved.</p>\n<p>But not the friction I hit.</p>\n<h2>The shape of the remaining gap</h2>\n<p>There are three sources of truth in any Cloudflare Worker project:</p>\n<ol>\n<li>The <strong>code</strong> — <code>env.CHAT_DB</code>, <code>env.UPLOADS_BUCKET</code>. <em>What the program needs.</em></li>\n<li>The <strong>wrangler config</strong> — <code>[[d1_databases]] binding = &quot;CHAT_DB&quot; database_id = &quot;…&quot;</code>. <em>What we&#39;ve declared.</em></li>\n<li>The <strong>account</strong> — actual D1 databases, R2 buckets, KV namespaces, queues. <em>What&#39;s real.</em></li>\n</ol>\n<p>These three drift constantly. Code references something that isn&#39;t declared. Config declares something that doesn&#39;t exist. The account holds something nothing references. The agent can read all three. But nothing reconciles them.</p>\n<p>Wrangler is the <em>build tool</em>. The 2500-endpoint API MCP is the <em>primitive layer</em>. The Skills plugin is the <em>recipe book</em>. There&#39;s no <em>reconciler</em> — no <code>git status</code> for bindings, no <code>kubectl apply</code> for a Worker project.</p>\n<p>So the agent&#39;s loop is:</p>\n<blockquote>\n<p>write code → run wrangler → read error → call MCP to look up resource → call MCP to create resource → ask the human to paste the ID → wait → wait → wait</p>\n</blockquote>\n<p>The wait is the failure. Every wait is the agent-flow leaking out into a human-flow.</p>\n<h2>Binding Doctor</h2>\n<p>I wrote <code>binding-doctor</code> (<code>bdr</code>) over a weekend. It&#39;s tiny — six TypeScript files, ~600 lines — and it does exactly one thing:</p>\n<pre><code>$ bdr diff\nBINDING         KIND     DECL  REF  LIVE  STATUS\n--------------  -------  ----  ---  ----  ----------------\nCHAT_DB         unknown  —     yes  —     missing-declared\nSESSIONS_KV     unknown  —     yes  —     missing-declared\nUPLOADS_BUCKET  unknown  —     yes  —     missing-declared\n</code></pre>\n<p>Three-way diff. Code says you need these. Config doesn&#39;t declare them. Account doesn&#39;t have them.</p>\n<pre><code>$ bdr apply --yes\n  create d1   CHAT_DB        → my-app-chat-db-8i6x   (9c48b532-…)\n  create kv   SESSIONS_KV    → my-app-sessions-kv-3h0f\n  create r2   UPLOADS_BUCKET → my-app-uploads-bucket-s000\n\nWrote 3 bindings to wrangler.toml.\nMigrations [CHAT_DB]: applied 0001_init.sql\n</code></pre>\n<p>90-second cast: <a href=\"https://asciinema.org/a/dYDfRnAlD7Cdzsth\">asciinema.org/a/dYDfRnAlD7Cdzsth</a>.</p>\n<p>It&#39;s idempotent. Re-run, zero diff. Delete the KV namespace from the dashboard, re-run, drift detected, drift healed. Same as Terraform — but for a Worker project, in one command, with no state file, because the wrangler config <em>is</em> the state file.</p>\n<p>There&#39;s also an MCP server (<code>bdr-mcp</code>) that exposes <code>bdr_diff</code>, <code>bdr_plan</code>, <code>bdr_apply</code>, <code>bdr_migrate</code> as tools. Plug it into Claude Code, and now the agent&#39;s loop becomes:</p>\n<blockquote>\n<p>write code → call bdr_apply → ship</p>\n</blockquote>\n<p>No human in the middle.</p>\n<h2>Why it&#39;s small on purpose</h2>\n<p><code>bdr</code> isn&#39;t a platform. It&#39;s not Pulumi. It&#39;s not a competitor to anything. It&#39;s the missing line between two existing primitives:</p>\n<ul>\n<li>the API MCP (which lets you create D1 databases imperatively)</li>\n<li>Wrangler (which deploys whatever&#39;s already declared)</li>\n</ul>\n<p>The line in between — <em>figure out what&#39;s missing and fix it</em> — was the manual step. I closed it with a regex (<code>env\\.([A-Z_]+)</code>), a TOML parser, a few <code>fetch</code> calls, and an inference table that maps <code>_DB → d1</code>, <code>_BUCKET → r2</code>, <code>_KV → kv</code>, <code>_QUEUE → queue</code>, <code>_INDEX → vectorize</code>.</p>\n<p>That&#39;s the whole product.</p>\n<h2>What I think Cloudflare should consider</h2>\n<p>This kind of thing should be in <code>wrangler</code> itself. Maybe <code>wrangler doctor</code>. Maybe a flag on <code>wrangler deploy</code> — <code>--auto-bind</code> — that does the same thing on the way to deploy. The MCP variant should ship as part of the Skills plugin.</p>\n<p>Three reasons it belongs in the platform:</p>\n<ol>\n<li><strong>It eliminates the most common agent-to-human handoff.</strong> Every other agentic flow Cloudflare shipped this year converges on this same friction point.</li>\n<li><strong>The data is all there.</strong> <code>wrangler.toml</code> is structured. The API has list endpoints. The code is parseable. There&#39;s nothing exotic.</li>\n<li><strong>It&#39;s a lever for adoption.</strong> The story &quot;Cloudflare is the only cloud where an agent ships an app without asking you for an ID&quot; is a story.</li>\n</ol>\n<h2>What&#39;s next</h2>\n<p><code>bdr</code> is on GitHub: github.com/diogodebastos/binding-doctor. MIT-licensed. The 90-second demo is in the repo. PRs welcome — vectorize is functional but the dimensions/metric should come from a code annotation, queues should auto-wire consumers when a <code>queue</code> handler is found, secrets need a <code>.env.doctor</code> template path. All weekend-2 work.</p>\n<p>I&#39;d love to build the next version of this inside Cloudflare. The agent-first pitch I keep hearing in the keynotes is the pitch I want to ship. If anyone reading this knows the right person — DevRel, Workers, Agents — I&#39;m at <a href=\"mailto:diogodebastos18@gmail.com\">diogodebastos18@gmail.com</a>.</p>\n<hr>\n<p><em>Thanks for reading. The repo: <a href=\"https://github.com/diogodebastos/binding-doctor\">github.com/diogodebastos/binding-doctor</a>. The 90-second demo: <a href=\"https://asciinema.org/a/dYDfRnAlD7Cdzsth\">asciinema.org/a/dYDfRnAlD7Cdzsth</a>.</em></p>\n"
  },
  "blogpost-4": {
    "title": "LLMs as Resistors: How Far Does the Circuit Analogy Stretch?",
    "excerpt": "Wiring language models in series and parallel — and dragging in capacitors and inductors when one LLM at a time wasn't enough.",
    "minutes": 10,
    "html": "<h1>LLMs as Resistors: How Far Does the Circuit Analogy Stretch?</h1>\n<p><em>Wiring language models in series and parallel — and dragging in capacitors and inductors when one LLM at a time wasn&#39;t enough.</em></p>\n<hr>\n<p>I spent a weekend wiring language models like circuit components. The result is <a href=\"https://github.com/diogodebastos/llm-circuits\">llm-circuits</a>: each LLM is a resistor, your prompt is voltage, you compose them with fork/join wiring. The whole thing is one Cloudflare Worker, and every &quot;current&quot; you apply is a real Workers AI call — no mock layer.</p>\n<p>The analogy mostly holds. Where it breaks is the interesting part.</p>\n<h2>The kit</h2>\n<ul>\n<li><strong>Voltage</strong> — the prompt.</li>\n<li><strong>Resistor</strong> — a model. R ∝ parameter count.</li>\n<li><strong>Series / Parallel</strong> — chained vs. fanned-out models with a join.</li>\n<li><strong>Capacitor</strong> — markdown-seeded memory. Injects context, absorbs output, persists per-node in <code>localStorage</code>.</li>\n<li><strong>Inductor</strong> — runs a model N times and votes. Resistance to <em>variance</em>.</li>\n<li><strong>Diode</strong> — one-way gate. Regex or judge-LLM check on the current text; if it fails, the branch is <em>blocked</em> (or passed through). Forward bias = approved output, reverse bias = refusal.</li>\n<li><strong>Transformer</strong> — small instructed model that reformats its branch&#39;s text (translate, summarise, JSON-ify) without changing the topology. Two coils, two voltages — same shape.</li>\n<li><strong>Ground</strong> — a refusal sink. A grounded branch drops out of the parallel join entirely; the vote/ensemble proceeds without it. Think &quot;this branch refused — count it as silent, not as a no.&quot;</li>\n</ul>\n<h2>Three modes for the same diagram</h2>\n<p>Forcing yourself to pick a semantics makes the analogy concrete instead of vibey:</p>\n<ul>\n<li><strong><code>refine-vote</code></strong> — series refines, parallel branches answer independently and a judge LLM picks consensus.</li>\n<li><strong><code>chain-ensemble</code></strong> — series feeds raw output forward, parallel synthesizes.</li>\n<li><strong><code>physics</code></strong> — R<sub>series</sub> = ΣR<sub>i</sub>, R<sub>parallel</sub> = 1/Σ(1/R<sub>i</sub>). Parallel branches split a fixed token budget <em>by conductance</em>, so a 3B paired with a 70B gets most of the budget — exactly like a current divider. Putting the math on screen is a much better intuition for load balancing than another paragraph of prose.</li>\n</ul>\n<h3>The token-budget formula, in one paragraph</h3>\n<p>Each model has a synthetic resistance R ∝ parameters (a 70B is &quot;more resistive&quot; than a 3B in the sense that it costs more to push a token through it). Conductance is G = 1/R. Given a budget B = 5120 tokens, branch <em>i</em> gets</p>\n<blockquote>\n<p>max_tokens<sub>i</sub> = round( G<sub>i</sub> / Σ G<sub>k</sub> · B )</p>\n</blockquote>\n<p>So in a parallel of <code>Llama-70B (R=70)</code> ‖ <code>Llama-3B (R=3)</code>, the 3B grabs 70/(70+3) ≈ 96 % of the budget and the 70B gets the remaining 4 %. That&#39;s the opposite of what you&#39;d intuit if you treated R as &quot;capacity&quot; — and it&#39;s the right call, because in current dividers <em>low-resistance branches carry more current</em>. The 3B has more headroom to ramble; the 70B is held to a tight, dense answer. In practice this is a surprisingly good heuristic for &quot;let the small model brainstorm, let the big model conclude.&quot; The whole point of putting <code>R = 1/G</code> on screen is that it forced me to defend that choice with a number, not a vibe.</p>\n<h2>Where it breaks</h2>\n<ul>\n<li>No Ohm&#39;s law. Models don&#39;t lose tokens proportionally to size.</li>\n<li>Sources combine non-trivially: two prompts into one LLM is routine; two voltage sources in parallel is a bug.</li>\n<li>No conservation law — which is exactly why ensembles work for LLMs and not resistors.</li>\n<li>Every parallel join is <em>another LLM</em>. There&#39;s no electrical equivalent of &quot;and now an op-amp arbitrates.&quot;</li>\n</ul>\n<p>The diagram is a teaching abstraction, not a predictive one. It earns its keep when it forces you to ask &quot;wait — what does parallel even mean here?&quot;</p>\n<h2>Presets that pushed the engine</h2>\n<p>Two-LLMs-in-parallel was enough to explain the vocabulary. Real circuits broke things in useful ways.</p>\n<p><strong>Code review</strong> — three reviewer branches (correctness, security, style), each with its own role-injected capacitor and a different model family so failures don&#39;t correlate, converging on a merge capacitor + merger model. To support it I had to:</p>\n<ul>\n<li>relax the validator to allow <strong>N source nodes</strong>, broadcasting the prompt across an implicit initial parallel stage,</li>\n<li>scope <strong>capacitor inject/absorb to a single branch</strong> instead of the parallel join,</li>\n<li>add a <strong>per-node <code>maxTokens</code> override</strong>, since the platform default truncates code-review-length outputs.</li>\n</ul>\n<p>Defaults to <code>physics</code> mode so the merger sees each reviewer&#39;s verbatim output instead of laundering it through the judge.</p>\n<p><strong>Best-of-four</strong> — four diverse models in parallel, a &quot;judge brief&quot; capacitor injects selection criteria into a final model that returns the single best answer. The smallest circuit that demonstrates why selection criteria want to live in an editable, version-controlled capacitor instead of hard-coded into the join.</p>\n<h2>BYOK</h2>\n<p>The demo originally ran every call through the project&#39;s Workers AI binding, which made it feel like a hosted toy. There&#39;s now a &quot;Connect Cloudflare&quot; modal: paste an account ID and API token, the front-end stores them in <code>localStorage</code> and dispatches a custom event so the canvas picks them up without a reload. The backend validates the account ID format and switches from the shared binding to the REST AI API when creds are present. Your circuits, your bill.</p>\n<h2>Implementation</h2>\n<p>One Worker. Astro builds the React Flow canvas as a client island and emits a single Worker entry serving static assets plus:</p>\n<ul>\n<li><code>POST /api/run</code> — <code>{ circuit, mode, prompt, capStates, seeds }</code>. Worker validates the topology (fork/join only, no cycles, capacitors only in series), walks the stages, dispatches each through <code>env.AI.run(...)</code> (or the REST API for BYOK), schedules capacitor effects forward and applies them around the next stage. Returns per-node trace + updated capacitor states.</li>\n<li><code>GET /api/capacitors</code> — serves the markdown content collection so the dropdown shows seed bodies.</li>\n<li>Real-time per-node status streamed over <strong>SSE</strong> so the canvas lights up node-by-node as the run progresses.</li>\n</ul>\n<p><code>wrangler.toml</code> declares <code>[ai] binding = &quot;AI&quot;, remote = true</code>, so even <code>wrangler dev</code> hits real Workers AI. CI auto-deploys on push to <code>main</code>.</p>\n<p>Front-end state lives in the URL hash as a base64url-encoded <code>{v:1, c:&lt;circuit&gt;}</code> — every wiring is a shareable link, and the version field means old links keep working when the schema changes. Capacitor state stays in <code>localStorage</code> on purpose: sharing transports the wiring, not your memory.</p>\n<h2>Extending the metaphor: Diode, Transformer, Ground</h2>\n<p>The first pass of the kit was just &quot;resistor + capacitor + inductor.&quot; That carries you to about <em>ensemble + memory + self-vote</em> and stops. Three new components opened up the cases I actually had in mind:</p>\n<p><strong>Diode — one-way gate.</strong> A node with two modes: a <code>regex</code> test, or a <code>judge</code> LLM that returns yes/no on a rubric you write. If the text passes, current flows. If it fails, you choose: <code>block</code> (cut the branch) or <code>passthrough</code> (annotate but continue). The interesting case is <em>judge-mode diodes inside a parallel stage</em>: each reviewer branch has its own gate (&quot;is this answer factually grounded?&quot;), and grounded-out branches don&#39;t get to vote.</p>\n<p><strong>Transformer — branch-local reformat.</strong> A small instructed model whose only job is to convert text — &quot;translate to French,&quot; &quot;extract the JSON,&quot; &quot;rewrite as a bullet list.&quot; Topologically it&#39;s a model node, but with the instruction baked in so the canvas stays readable. Think of the two coils as input and output domains: same energy, different representation.</p>\n<p><strong>Ground — refusal sink.</strong> A diode that fails into ground silently removes its branch from the parallel join. The vote proceeds with N−1 voters instead of N. This is the difference between &quot;this branch said no&quot; and &quot;this branch had nothing to say&quot; — which matters for consensus arithmetic in <code>refine-vote</code>.</p>\n<p>The reason these three earn their slots and &quot;op-amp&quot; and &quot;switch&quot; don&#39;t (yet): each maps to a single, common LLM-pipeline pattern (guardrail / format-shift / soft-refusal) and each has a <em>visible</em> electrical analogue. The op-amp version of feedback agents needs a notion of time-domain, which the canvas doesn&#39;t model — that&#39;s still future work.</p>\n<h2>AI Gateway: caching the bill</h2>\n<p>Workers AI calls go through the <code>env.AI</code> binding — easy, but opaque. Wrap that binding with <strong>AI Gateway</strong> (free tier: 100k logs/day) and you get caching, rate limiting, and per-call cost telemetry &quot;for free.&quot; <code>wrangler.toml</code> exposes a gateway as an optional env var:</p>\n<pre><code class=\"language-toml\">[ai]\nbinding = &quot;AI&quot;\nremote = true\n# gateway = { id = &quot;llm-circuits&quot; }   # uncomment after creating in CF dashboard\n</code></pre>\n<p>When set, every <code>env.AI.run(model, input, { gateway: { id, skipCache: false } })</code> is logged and cached. The result panel surfaces, per call:</p>\n<ul>\n<li><code>cached ✓</code> (1 ms response time, $0)</li>\n<li><code>neurons used</code> (what the call actually cost on Workers AI&#39;s pricing unit)</li>\n<li><code>latency</code></li>\n</ul>\n<p>Run a circuit twice with the same prompt, watch every node go green-with-a-cache-mark on the second run. This turns the canvas into something I&#39;d actually demo to someone evaluating Workers AI — <em>here&#39;s the cost dropping in real time, here&#39;s the cache key, here&#39;s the log entry in the dashboard.</em></p>\n<h2>Scaling capacitors: a Durable Objects design (not implemented — yet)</h2>\n<p><code>localStorage</code> capacitors are fine for one user. They&#39;re terrible for <em>team</em>-shared memory: a &quot;house style guide&quot; capacitor shared across collaborators, or a long-running scratchpad that survives across browsers, or a knowledge capacitor that two parallel branches <em>both</em> absorb into. None of that is solvable on the client.</p>\n<p>The clean Cloudflare answer is <strong>Durable Objects</strong>: each capacitor becomes one DO addressed by its slug, with strong single-writer consistency for free.</p>\n<pre><code class=\"language-toml\"># wrangler.toml — uncomment when on Workers Paid\n# [[durable_objects.bindings]]\n# name  = &quot;CAPS&quot;\n# class_name = &quot;Capacitor&quot;\n#\n# [[migrations]]\n# tag = &quot;v1&quot;\n# new_sqlite_classes = [&quot;Capacitor&quot;]\n</code></pre>\n<pre><code class=\"language-ts\">// worker/Capacitor.ts\nexport class Capacitor implements DurableObject {\n  constructor(private state: DurableObjectState) {}\n  async fetch(req: Request) {\n    const url = new URL(req.url);\n    if (req.method === &quot;GET&quot;)  return new Response(await this.state.storage.get&lt;string&gt;(&quot;text&quot;) ?? &quot;&quot;);\n    if (req.method === &quot;PUT&quot;)  { await this.state.storage.put(&quot;text&quot;, await req.text()); return new Response(&quot;ok&quot;); }\n    return new Response(&quot;method not allowed&quot;, { status: 405 });\n  }\n}\n\n// /api/cap/[id].ts\nconst id = env.CAPS.idFromName(params.id);\nreturn env.CAPS.get(id).fetch(req);\n</code></pre>\n<p>Why DO and not KV? KV is eventually consistent — fine for seeds, dangerous for absorb-then-inject in the same run. DO gives you <em>single-writer-per-id</em> serialization, so two parallel branches absorbing into the same capacitor produce a deterministic interleave instead of a last-writer-wins race.</p>\n<p>Why isn&#39;t it shipped? Durable Objects are a Workers Paid feature ($5/month minimum), and the explicit goal of this project is to stay free-tier. The wiring above is the entire diff — capacitor nodes get a &quot;share globally&quot; toggle, the runner switches the cap-state path from <code>localStorage</code> to <code>fetch(&quot;/api/cap/&quot; + slug)</code>, the rest of the pipeline is unchanged. The day this becomes worth $5, it&#39;s a one-commit flip.</p>\n<h2>Eval mode and MCP export</h2>\n<p>The two follow-on features that turn the canvas from a toy into a research artifact:</p>\n<p><strong>Compare-3-modes.</strong> A button that runs the same circuit under all three semantics in parallel and renders a table — <code>output | R_total | latency | tokens | neurons</code> per row. The point is to make the <em>same</em> topology answer the same question three ways and show you side-by-side how <code>physics</code> vs. <code>refine-vote</code> differ on a real prompt. Most of the time the verdict is &quot;they&#39;re close, refine-vote is slightly better, physics is cheaper&quot; — but the cases where they <em>disagree</em> are where prompt engineering decisions actually live.</p>\n<p><strong>Golden-answer eval.</strong> A capacitor with <code>role: &quot;golden&quot;</code> becomes the rubric for a judge LLM that scores each run 0–10 against the stored text. Cross with compare-3-modes and you get a scatter plot: R<sub>total</sub> on x-axis, quality on y-axis, three points per run. The question &quot;is the bigger circuit worth it?&quot; becomes a chart instead of a vibe.</p>\n<p><strong>MCP export.</strong> Any saved circuit can emit a JSON tool spec — name, input schema (the prompt), output schema (final text), embedded topology — that drops into a host MCP server as a single tool. Your handcrafted &quot;code-review-3-reviewer&quot; circuit becomes <code>circuit.codeReview</code> callable from any MCP-aware agent. The spec is portable; the runtime stays on Workers AI.</p>\n<h2>What&#39;s next</h2>\n<ul>\n<li>Transient analysis — replay the same prompt N times through a circuit with a capacitor and <em>plot</em> stored-text length vs. step, giving the analogy back its time-domain story.</li>\n<li>Op-amp / feedback agents (needs the time-domain piece first).</li>\n<li>Switches (tool-routers) — partway between diode and transformer; deferred until the demand is real.</li>\n</ul>\n<h2>Try it</h2>\n<pre><code class=\"language-bash\">git clone https://github.com/diogodebastos/llm-circuits\ncd llm-circuits\nnpm install\nnpm run wrangler:remote\n</code></pre>\n<p>The point isn&#39;t that LLMs are resistors. They aren&#39;t. The point is that two-terminal components are a <em>cheap</em> way to talk about composition, and we don&#39;t have many other cheap ways. The places this analogy breaks aren&#39;t bugs — they&#39;re the actual ways LLM systems differ from physical ones, and noticing them by playing with a wiring diagram beats reading another taxonomy.</p>\n"
  },
  "blogpost-3": {
    "title": "Using Claude Code to create a Pokémon RomHack",
    "excerpt": "The idea: make the version of Pokémon Emerald that I wanted to play as a kid.",
    "minutes": 4,
    "html": "<h1>Using Claude Code to create a Pokémon RomHack</h1>\n<p><strong>The idea:</strong> make the version of Pokémon Emerald that I wanted to play as a kid.</p>\n<p><strong>The how:</strong> use <a href=\"https://github.com/pret/pokeemerald\">pokémon emerald decompilation project</a>, mods from the romhacking community (<a href=\"https://github.com/cRz-Shadows/Pokemon_Emerald_Legacy\">Pokémon Emerald Legacy</a> and <a href=\"https://github.com/Exclsior/Pokemon_Emerald_Legacy_Enhanced\">enhanced version</a>), Claude Code to help me stitch things together and add my own custom modifications, and Cloudflare to host the game for free.</p>\n<p><img src=\"/images/blog/bp3/pokeemerald-0.png\" alt=\"title screen\"></p>\n<h2>Solo Leveling</h2>\n<p>You are the only trainer in Hoenn that can level up. Your objective is to beat every trainer, but if you lose, you start from your hometown again. You only get to keep your team and bag. Pokémons follow you! </p>\n<p>Included features:</p>\n<ul>\n<li>massively diversified trainer teams</li>\n<li>special trainers from other regions</li>\n<li>dynamic level scaling</li>\n<li>extensive quality-of-life improvements, including all pokemon available, stats editor, and special NPCs across the region</li>\n</ul>\n<p>Talk to Mom in Littleroot Town. She explains the rules and activates/deactivates the mode.</p>\n<p><img src=\"/images/blog/bp3/pokeemerald-7.png\" alt=\"title screen\">\n<img src=\"/images/blog/bp3/pokeemerald-8.png\" alt=\"title screen\">\n<img src=\"/images/blog/bp3/pokeemerald-9.png\" alt=\"title screen\"></p>\n<h2>Colosseum</h2>\n<p>Every battle is a double battle. You start with Espeon and Umbreon or with two Eevees.</p>\n<p><img src=\"/images/blog/bp3/pokeemerald-10.png\" alt=\"title screen\">\n<img src=\"/images/blog/bp3/pokeemerald-1.png\" alt=\"title screen\">\n<img src=\"/images/blog/bp3/pokeemerald-2.png\" alt=\"title screen\"></p>\n<h2>Pokémon Emerald Legacy</h2>\n<p>This is one of the best romhacks out there, with a huge amount of content and quality-of-life improvements. I used it as the base. A big thank you to The SmithPlays Team. You guys rock!</p>\n<p><img src=\"/images/blog/bp3/pokeemerald-5.png\" alt=\"title screen\">\n<img src=\"/images/blog/bp3/pokeemerald-4.png\" alt=\"title screen\">\n<img src=\"/images/blog/bp3/pokeemerald-3.png\" alt=\"title screen\"></p>\n<h2>Claude</h2>\n<p>The romhack itself is built with Claude Code. The pokeemerald decompilation is ~1M lines of C, navigating it, understanding the data structures (trainer parties, scripts, map events, battle engine), and cross-referencing the two upstream forks would have taken months solo. Claude Code did the heavy lifting.</p>\n<p>What it actually did on this repo:</p>\n<ul>\n<li><strong>Solo Leveling mode</strong>: wired the toggle into the Mom NPC script, patched the trainer encounter flow to respawn the player in Littleroot on loss while preserving team and bag, and hooked into the level-scaling logic so only the player gains XP.</li>\n<li><strong>Colosseum mode</strong>: converted single encounters to double battles, added the Espeon/Umbreon and dual-Eevee starter branch, adjusted catch logic for double battles, and extended Eevee evolution learnsets.</li>\n<li><strong>Following Pokémon</strong>: ported the overworld follower sprite system, the diff touches <code>overworld.c</code>, <code>field_player_avatar.c</code>, and <code>bike.c</code> to keep the follower sprite coherent through biking, surfing, and map transitions.</li>\n<li><strong>QoL</strong>: complete move relearner, stat editor with shiny toggle and Poké Ball edit, stat editor for contest stats, and a bunch of battle-engine bug fixes (BR HUD memory corruption, corrupted vars, acro bike ledge jump, pomeg-berry Shedinja).</li>\n<li><strong>Web</strong>: the landing page, embedded Pokédex, and online play wrapper around the <code>.gba</code> build, all generated and iterated on with Claude.</li>\n</ul>\n<p>Workflow: describe the feature, point Claude at the relevant subsystem (<code>data/trainers.party</code>, <code>src/battle_*.c</code>, <code>src/overworld.c</code>), let it propose a patch, compile, test in mGBA, iterate. Commit history on <a href=\"https://github.com/diogodebastos/Pokemon_Emerald_Legacy\">the repo</a> shows the progression.</p>\n<p><img src=\"/images/blog/bp3/pokeemerald-6.png\" alt=\"title screen\">\n<img src=\"/images/blog/bp3/pokeemerald-11.png\" alt=\"title screen\">\n<img src=\"/images/blog/bp3/pokeemerald-13.png\" alt=\"title screen\"></p>\n<p>But if I have to summarize using Claude Code in one image, is this:</p>\n<p><img src=\"/images/blog/bp3/red01.jpg\" alt=\"title screen\"></p>\n<p><em>&quot;From comment to feature in an hour&quot;</em></p>\n<h2>Cloudflare</h2>\n<p>Cloudflare is what makes a project like this viable for a solo hobbyist. A ROM hack is a weird thing to ship: a <code>.gba</code> blob, a JS emulator, a Pokédex, a landing page, and the occasional experimental feature. No revenue, no enterprise budget, just me on a laptop wanting strangers to be able to click a link and play. Cloudflare Pages hosts it all, free, globally distributed, zero config beyond pointing it at the git repo. Push to main, it deploys. That is the entire ops story.</p>\n<p>What I really appreciate is that Cloudflare treats open source and hobbyist work as first-class. The free tier is not a crippled demo, it is the real product. That matters.</p>\n<p>The workflow also maps cleanly onto agentic development. I iterate on the ROM and the site together, Claude Code edits C in the decomp, rebuilds the <code>.gba</code>, and simultaneously updates the Astro site and Pokédex data in the edge repo. Pages picks up the push and deploys. The agent never needs to think about infrastructure, which is exactly what you want when the human-in-the-loop is reviewing feature diffs and not YAML. Shorter feedback loop, more features shipped, fewer things to hold in my head.</p>\n<p>If someone at Cloudflare is reading: thank you. And if you are building something agentic on top of Workers/Pages/Durable Objects and want to talk to a builder who has spent real time orchestrating Claude Code across a multi-repo, multi-language project (C decomp + TypeScript/Astro frontend + emulator glue), <a href=\"https://github.com/diogodebastos\">get in touch</a>.</p>\n<p><img src=\"/images/blog/bp3/pages01.png\" alt=\"title screen\">\n<img src=\"/images/blog/bp3/pages02.png\" alt=\"title screen\"></p>\n<h2>Play it here!</h2>\n<p><a href=\"https://pokemon-emerald-legacy-solo-leveling-colosseum.pages.dev/\">Pokemon Emerald Legacy Solo Leveling Colosseum</a> website with embedded pokédex.</p>\n<p><a href=\"https://www.hackdex.app/hack/pokemon-emerald-legacy-solo-leveling-colosseum\">HackDex</a></p>\n<p>Contribute <a href=\"https://github.com/diogodebastos/Pokemon_Emerald_Legacy\">here</a> on GitHub.</p>\n<p><img src=\"/images/blog/bp3/pokegba.png\" alt=\"title screen\"></p>\n"
  },
  "blogpost-2": {
    "title": "[wip] Claude Takes Control of My LinkedIn",
    "excerpt": "Claude Control Panel",
    "minutes": 1,
    "html": "<h1>[wip] Claude Takes Control of My LinkedIn</h1>\n<h2>The control panel</h2>\n<p><a href=\"https://cf-cl-lin-2.diogobastos.workers.dev/\">Claude Control Panel</a></p>\n<h2>Cloudflare is human-first</h2>\n<p>D1 and Workers, great for builders and PoC projects, no gatekeeping</p>\n<h2>Skills</h2>\n<p>talk about your skills</p>\n<h2>MCP - Playwrite</h2>\n<p>If you can access browser you can do everything</p>\n<h2>Automation is just cron jobs</h2>\n<p>cronjobs are the heart beat of ai automation</p>\n<h2>X (Twitter) account</h2>\n<p>improving twitter </p>\n<h2>LinkedIn Quick Saga</h2>\n<p>account banned, linkedin is strong against automation/bots but this just makes linkeding a good testing ground, benchmark, web interaction turing test maybe?</p>\n<p><img src=\"/images/blog/bp2/lin_run.png\" alt=\"LinkedIn Runnig\"></p>\n<p><img src=\"/images/blog/bp2/lin_baned.png\" alt=\"LinkedIn Restricted\"></p>\n<h2>No shame LLM</h2>\n<p>claude is ok in going to Matt (CEO of cloudflare) and just saying hi and shamelessly pluging </p>\n<h2>Token poor</h2>\n<p>tokens are the limitiatio right now and a good place to look how this will impact future ai adoption, is big tech going to gatekeep? will open source models run locally?</p>\n<h2>wasting tokens on bad loops</h2>\n<p>sometimes you can see claude doing some mistake a human would never do and wasting it self in loops trying to fix it - example: post had surpass the characters allowed and it just got stuck</p>\n<h2>Security issues</h2>\n<h2>Conclusion</h2>\n<p>fun project, using cloudflare infrastructure, automating claude with cronjobs, creating a dashboard to help the llm, working with skills, seeing the browser being autonamasly controlled. the objective is not to get hired by cloudflare in lisbon, that would be nice, it was just a fun project to do and to see the sota of llms. new ideas are forming... optimize for fun!</p>\n"
  },
  "blogpost-1": {
    "title": "[wip] Building a CV Chatbot with Django and Claude",
    "excerpt": "Recruiters skim CVs in seconds. What if they could just ask questions instead? That's the idea behind cavai — a conversational interface powered by Claude that knows everything…",
    "minutes": 1,
    "html": "<h1>[wip] Building a CV Chatbot with Django and Claude</h1>\n<h2>Why a chatbot for a CV?</h2>\n<p>Recruiters skim CVs in seconds. What if they could just ask questions instead? That&#39;s the idea behind cavai — a conversational interface powered by Claude that knows everything about my background.</p>\n<h2>The stack</h2>\n<ul>\n<li><strong>Django</strong> for the backend — simple, batteries-included, perfect for a quick deploy</li>\n<li><strong>Claude Sonnet</strong> via the Anthropic API — handles the conversation with my CV as context</li>\n<li><strong>Vercel</strong> for hosting — free tier, zero config, just push and deploy</li>\n<li><strong>Markdown</strong> for content — the CV and blog posts are plain <code>.md</code> files, converted to HTML on the fly</li>\n</ul>\n<h2>How it works</h2>\n<p>The CV is loaded as a markdown file at server start. When a user sends a message, it&#39;s appended to a conversation history along with the CV content as system context. Claude responds based only on what&#39;s in the CV — no hallucinations about skills I don&#39;t have.</p>\n<h2>Keeping it lean</h2>\n<p>No database for content. No CMS. No build step. Just markdown files in a templates folder, a few Django views, and the Anthropic SDK. The whole thing is under 500 lines of Python.</p>\n<h2>What&#39;s next</h2>\n<ul>\n<li>More blog posts about projects and experiments</li>\n<li>Maybe a voice interface?</li>\n<li>Exploring local models as an alternative to API calls</li>\n</ul>\n<h2>Conclusion</h2>\n<p>Sometimes the best portfolio is one that talks back. If you&#39;re reading this, try asking the chatbot something — it might surprise you.</p>\n"
  }
};
export default posts;
